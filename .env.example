# Azure AI Foundry (server-side only)
AZURE_AI_ENDPOINT=
AZURE_AI_KEY=
AZURE_AI_DEPLOYMENT=
AZURE_COG_ENDPOINT=
AZURE_SPEECH_REGION=eastus

# SBI InnoHub Banking APIs (from the API console "X-Authorization" box, include "Bearer ")
SBI_API_BASE=https://api.innohub.sbi
SBI_API_TOKEN=

# leave empty on the sandbox — gate off, judges get straight in
SITE_PASSWORD=
SBI_IH_CODE=000073
